import { FontSizes } from "../../types";

const Sizes: FontSizes = {
    medium: {
        normal: {
            MEDIUM_FONT: '1.5rem',
        }
    },
    large: {
        normal: {
            LARGE_FONT: '2rem',
        }
    }
};

export default Sizes;