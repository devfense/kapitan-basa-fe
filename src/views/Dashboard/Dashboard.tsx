import React from 'react';
import { Container } from '../../globalStyles';

const Dashboard = () => {
	return (
		<Container>
            This is Dashboard
		</Container>
	);
};

export default Dashboard;
