
interface Routes {
    title: string;
    route: string;
    key: string;
};

export default Routes;

export interface DataHeader {
    header?: string;
};

export interface DataItem {
    dataItem: string;
}