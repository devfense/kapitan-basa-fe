
export interface LocaleStrings {
    app: string,
    regLabel: string,
    asStudent: string;
    dashboard: string;
    delete: string;
    edit: string;
    gameLevel: string;
    login: string;
    name: string;
    tabResults: string;
    userMgmt: string;
    appDesc: string;
}

export type Languages = 'en' | 'tagalog';

export interface LanguageStrings {
    en: LocaleStrings;
}