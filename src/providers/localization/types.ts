
export interface LocaleStrings {
    title: string;
    subTitle: string,
    regLabel: string,
    asStudent: string;
    dashboard: string;
    delete: string;
    edit: string;
    gameLevel: string;
    login: string;
    name: string;
}

export type Languages = 'en' | 'tagalog';

export interface LanguageStrings {
    en: LocaleStrings;
}